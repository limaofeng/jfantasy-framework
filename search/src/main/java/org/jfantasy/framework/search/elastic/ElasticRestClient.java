package org.jfantasy.framework.search.elastic;

// import org.elasticsearch.client.RestHighLevelClient;
// import org.elasticsearch.client.RestHighLevelClientBuilder;

public class ElasticRestClient {

  public void test() {
    // Create the low-level client
    //    RestClient httpClient = RestClient.builder(new HttpHost("101.132.33.180", 9200)).build();
    //
    //    // Create the HLRC
    //    RestHighLevelClient hlrc =
    //        new RestHighLevelClientBuilder(httpClient).setApiCompatibilityMode(true).build();
    //
    //    // Create the Java API Client with the same low level client
    //    ElasticsearchTransport transport =
    //        new RestClientTransport(httpClient, new JacksonJsonpMapper());
    //
    //    ElasticsearchClient esClient = new ElasticsearchClient(transport);
  }
}
