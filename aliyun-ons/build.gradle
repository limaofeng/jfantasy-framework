description = ' 阿里云消息队列 '

sonarqube {
    skipProject = true
}

dependencies {
    compile project(':jfantasy-core')
    compile "com.aliyun.openservices:ons-client:1.2.1"
    testCompile "org.springframework.boot:spring-boot-starter-test:${springBootVersion}"
}
